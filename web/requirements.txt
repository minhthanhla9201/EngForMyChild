# Web app (Django) — GĐ 0
Django>=5.2,<5.3
python-dotenv>=1.0
dj-database-url>=2.1

# GĐ 1 — từ vựng + audio/IPA
Pillow>=10.0          # ImageField (hình từ vựng)
edge-tts>=6.1         # sinh audio TTS (cần mạng; đã cache)
pyttsx3>=2.90         # TTS offline dự phòng (không cần mạng)
eng-to-ipa>=0.0.2     # sinh phiên âm IPA

# GĐ 3 — chấm phát âm: web gọi service ASR (faster-whisper) qua HTTP
requests>=2.31       # gọi service ASR

# Chỉ cần khi chạy MySQL (xem docs/ThietKeDuLieu.md mục 0.1):
# mysqlclient>=2.2
whitenoise>=6.6      # phục vụ file tĩnh khi DEBUG=False
waitress>=3.0        # WSGI server cho Windows (Option 1)